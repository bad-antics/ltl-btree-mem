// examples/demo.ltl — runnable demo for ltl_btree_mem

import std::io
use ltl_btree_mem as lib

fn main() {
    io::println("ltl_btree_mem demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}