// ltl-btree-mem/main.ltl
//
// In-memory B-tree index with range cursors.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-btree-mem v0.1.0")
    io::println("In-memory B-tree index with range cursors.")
    0
}
